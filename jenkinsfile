#!/usr/bin/env groovy

node {
	stage('Environment Setup') {
		properties([
			buildDiscarder(logRotator(numToKeepStr: '10'))
		])
	}
	stage('SCM Checkout') {
			checkout(scm)
		}
    stage('ECR Repo Init') {
        git url: 'git@github.com:roeP/MI-infra.git'
        dir('MI-infra') {
            sh 'ls'
        }
    }

    stage('Docker Build') {

    }

    stage('Docker Push') {

    }
}
